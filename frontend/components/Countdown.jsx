let React = require('react');

let Countdown = React.createClass({
    render: function () {
        return (
            <div>
                Countdown Component
            </div>
        );
    }
});

module.exports = {Countdown};